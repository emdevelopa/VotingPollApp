/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["res.cloudinary.com", "images.unsplash.com"], // Add Cloudinary's hostname here
  },
};

export default nextConfig;
